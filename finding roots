#include <stdio.h>
#include<math.h>

int main()
{
float a,b,c;
scanf("%f %f %f",&a,&b,&c);
float d=b*b-4*a*c;
if(d<0){
printf(" roots are imaginary");
}
else{
float r1=(-b+sqrt(b*b-4*a*c))/(2*a);
float r2=(-b-sqrt(b*b-4*a*c))/(2*a);
printf("roots are:\n%f\n%f",r1,r2);
}
}
